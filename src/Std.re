
include Prelude;
module Async = Async;

module Option = {
  include StdOption;
};

module Remote = {
  include StdRemote;
};

module Result = {
  include StdResult;
};

module List = {
  include StdList;
};

module Dict = {
  include StdDict;
};

module String = {
  include StdString;
};
